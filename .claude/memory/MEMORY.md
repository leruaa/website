# Memory Index

- [Use bun instead of npm](feedback_bun.md) — user uses bun as package manager; use `~/.bun/bin/bun add` not `npm install`
- [Print page breaks inside flex](feedback_print_flex.md) — break-after/before ignored in flex containers; switch to `display: block` at print time
